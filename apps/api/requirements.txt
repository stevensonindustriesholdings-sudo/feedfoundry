# Production API dependencies (kept in sync with apps/api/pyproject.toml [project.dependencies]).
# The API Dockerfile installs from this file.
#
# Local dev / pytest: create `apps/api/.venv`, then:
#   pip install -r requirements.txt && pip install -e ".[dev]"
# so `stripe`, `pytest`, and the `app` package resolve (avoids ModuleNotFoundError on fresh clones).
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sqlmodel>=0.0.22
sqlalchemy>=2.0.0
# Railway Postgres often uses postgresql://… (SQLAlchemy → psycopg2 driver)
psycopg2-binary>=2.9.0,<3.0.0
# Recommended URL in .env.example: postgresql+psycopg://… (psycopg v3)
psycopg[binary]>=3.2.0
pydantic-settings>=2.4.0
pyyaml>=6.0.2
httpx>=0.27.0
boto3>=1.35.0
alembic>=1.14.0
stripe>=11.0.0,<16.0.0
