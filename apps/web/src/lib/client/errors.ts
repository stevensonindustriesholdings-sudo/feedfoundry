/** Re-export for `api.ts` colocated imports; implementation lives in `src/lib/errors.ts`. */
export type { ClientError, ClientErrorCode } from "../errors";
export { mapUpstreamError } from "../errors";
