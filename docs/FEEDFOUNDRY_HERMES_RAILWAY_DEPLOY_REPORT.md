# FeedFoundry Hermes Railway Deploy Report

Date: 2026-05-18
Execution lane: hermes_terminal_deployment_operator

## Summary

Hermes linked the FeedFoundry Railway project/environment, deployed the launch MVP branch to the API and worker services, set the requested launch MVP feature flags, and stopped at the migration phase because the safe Alembic upgrade command could not resolve Railway's private Postgres hostname from the local `railway run` execution context.

No Stripe configuration was touched. No destructive database commands were run. No FeedFoundry production reset/drop/truncate/downgrade command was run. No branch was merged. No PR was created. No push to `main` was performed.

## Git state

Primary repo:

```text
/Users/stevelee/Documents/feedfoundry
```

Branch:

```text
feat/feedfoundry-launch-mvp-flow
```

Commit SHA deployed:

```text
2d36c1c30013fab7505102817669060ef508b55f
```

Recent commit:

```text
2d36c1c feat: add FeedFoundry launch MVP intake flow
```

Remote branch verification:

```text
origin/feat/feedfoundry-launch-mvp-flow -> 2d36c1c30013fab7505102817669060ef508b55f
```

Initial untracked files found before deployment:

```text
.cursor/rules/feedfoundry-hermes-agent-proof.mdc
apps/api/uv.lock
docs/FEEDFOUNDRY_HIGH_END_UI_UX_PROTOTYPE_SPEC.md
```

Those were stashed before deployment with:

```bash
git stash push -u -m "pre-hermes-railway-deploy-untracked-feedfoundry"
```

A new untracked `apps/api/uv.lock` was generated by the later `uv run` migration attempt. It was not staged or committed.

## Railway link

Railway CLI:

```text
railway 4.58.0
```

Railway user:

```text
Steve Lee
```

Linked project/environment:

```text
Workspace: Steve Lee's Projects
Project: FeedFoundry - V0.1
Project ID: 2561baf3-d03e-4179-b74d-bfd2fdc8cea4
Environment: production
Environment ID: 85288e72-46a5-404d-ae1f-90edc3dca316
```

Link command run:

```bash
railway link --project 2561baf3-d03e-4179-b74d-bfd2fdc8cea4 --environment production
```

No new Railway project was created.

## Services detected

Detected FeedFoundry services in Railway:

| Service | Service ID | Result |
|---|---|---|
| api-v2-IQho | cb2e0aff-f38b-4323-b0ac-f1c83be19345 | Online / SUCCESS |
| worker-v2 | b1649486-6cba-4697-9429-cd7619f9bb71 | Online / SUCCESS |
| migrate | cb7e9064-ef31-47b8-93d5-d4a884781ab9 | Completed / SUCCESS |
| feedfoundry-web | 701cc759-4d9f-42b1-bc64-8a4a3eec25f5 | Existing web service; not deployed by this run |
| Postgres-RhhK | 36ff24eb-4284-42d5-84df-07c39a4cab46 | Online |
| Redis-GvIO | 46c8e168-c732-4254-b7de-8e89f6fe790b | Online |

Expected services matched actual service names:

```text
API: api-v2-IQho
Worker: worker-v2
```

## Deploy commands run

API deploy:

```bash
railway up --service api-v2-IQho --detach --message "Hermes launch MVP deploy API 2d36c1c"
```

API deployment observed after deploy:

```text
Deployment ID: 2c77cdab-8b53-496c-8dae-3391008a8d9d
Status: SUCCESS
URL: https://api-v2-iqho-production.up.railway.app
```

Worker deploy:

```bash
railway up --service worker-v2 --detach --message "Hermes launch MVP deploy worker 2d36c1c"
```

Worker deployment observed after deploy:

```text
Deployment ID: a4a7a361-8c52-42bd-8752-42aef3cde445
Status: SUCCESS
```

## Feature flags set

API service feature flag set:

```text
FF_YOUTUBE_SOURCE_ACQUISITION_ENABLED=1
```

Worker service feature flag set:

```text
FF_FEEDFOUNDRY_AGENT_BUNDLE_ENABLED=1
```

Commands run:

```bash
railway variable set FF_YOUTUBE_SOURCE_ACQUISITION_ENABLED=1 --service api-v2-IQho
railway variable set FF_FEEDFOUNDRY_AGENT_BUNDLE_ENABLED=1 --service worker-v2
```

Variable-name verification, with values not printed:

| Service | Variable | Present |
|---|---|---|
| api-v2-IQho | FF_YOUTUBE_SOURCE_ACQUISITION_ENABLED | yes |
| worker-v2 | FF_FEEDFOUNDRY_AGENT_BUNDLE_ENABLED | yes |
| api-v2-IQho | FF_YOUTUBE_SOURCE_ACQUISITION_LIVE | no |
| worker-v2 | FF_WORKER_AI_ENRICHMENT_ENABLED | yes, pre-existing/not set by this run |

Important: `FF_WORKER_AI_ENRICHMENT_ENABLED` was observed as already present on `worker-v2`. Hermes did not set it in this run.

## Migration

Required migration:

```text
Alembic 010_launch_mvp_intake_fields
```

Safe command attempted:

```bash
railway run --service api-v2-IQho -- bash -lc 'cd apps/api && PYTHONPATH=. uv run --python 3.11 python -m alembic -c alembic.ini upgrade head'
```

Migration result:

```text
FAILED
```

Failure reason:

```text
psycopg2.OperationalError: could not translate host name "postgres-rhhk.railway.internal" to address: nodename nor servname provided, or not known
```

Interpretation:

`railway run` in this CLI version runs a local command with Railway variables. The command received the private Railway Postgres hostname, but that hostname is resolvable only inside Railway's private network, not from the local macOS runtime. Because of the hard safety rule, Hermes stopped after this failure and did not attempt manual SQL or any destructive workaround.

No downgrade/reset/drop/truncate command was run.

## Health checks and smoke

Not run after migration failure. The instruction for this deployment was to stop and report the exact migration error if the migration failed.

Planned health checks after migration succeeds:

```bash
curl -fsS https://api-v2-iqho-production.up.railway.app/health
curl -fsS https://api-v2-iqho-production.up.railway.app/ready
curl -fsS https://api-v2-iqho-production.up.railway.app/openapi.json | python3 - <<'PY'
import sys, json
data=json.load(sys.stdin)
paths=data.get("paths", {})
for p in [
    "/v1/intake/youtube-video",
    "/v1/intake/youtube-playlist",
    "/v1/intake/upload",
    "/v1/jobs",
]:
    print(p, "YES" if p in paths else "NO")
PY
```

Non-destructive live smoke status:

```text
NOT_RUN: blocked by migration failure.
```

## Blockers

1. Migration could not run through local `railway run` because Railway private DNS for Postgres is not resolvable locally.
2. Need a migration execution path inside Railway's private network. The existing `migrate` service may be the intended path, but Hermes did not mutate it or redeploy it after the safe API-service `railway run` migration failed.
3. `FF_WORKER_AI_ENRICHMENT_ENABLED` is already present on `worker-v2`; Steve should confirm whether that should remain enabled for launch MVP.
4. Health/ready/OpenAPI checks were not run because the instructions required stopping after migration failure.

## Exact next commands for Steve

Recommended next safe step is to run the migration from inside Railway's network using the existing `migrate` service or a Railway shell/SSH session attached to a service that can resolve `postgres-rhhk.railway.internal`.

If using the existing `migrate` service is approved, inspect first:

```bash
cd /Users/stevelee/Documents/feedfoundry
railway service status --service migrate || true
railway logs --service migrate --lines 100 || true
```

Then use Railway's dashboard or approved CLI path to run only:

```bash
cd apps/api && PYTHONPATH=. uv run --python 3.11 python -m alembic -c alembic.ini upgrade head
```

Do not run downgrade/reset/drop/truncate.

After migration succeeds, run:

```bash
cd /Users/stevelee/Documents/feedfoundry
curl -fsS https://api-v2-iqho-production.up.railway.app/health
curl -fsS https://api-v2-iqho-production.up.railway.app/ready
curl -fsS https://api-v2-iqho-production.up.railway.app/openapi.json | python3 - <<'PY'
import sys, json
data=json.load(sys.stdin)
paths=data.get("paths", {})
for p in [
    "/v1/intake/youtube-video",
    "/v1/intake/youtube-playlist",
    "/v1/intake/upload",
    "/v1/jobs",
]:
    print(p, "YES" if p in paths else "NO")
PY
```

## Hermes proof block

```json
{
  "hermes_acp_check": "PASS",
  "hermes_smoke": "NOT_RUN",
  "execution_lane": "hermes_terminal_deployment_operator",
  "direct_openai_or_openrouter_used": false,
  "agent_stack_or_skill_used": true,
  "agent_stack_or_skill_name": "deployment_operator",
  "proof_artifacts": [
    "docs/FEEDFOUNDRY_HERMES_RAILWAY_DEPLOY_REPORT.md"
  ],
  "notes": "Railway link/deploy/flag setup reached; migration blocked because local railway run could not resolve Railway private Postgres DNS."
}
```
